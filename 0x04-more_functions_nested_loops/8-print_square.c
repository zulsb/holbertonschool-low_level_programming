#include "holberton.h"

/**
 * print_square - Print square..
 *@size: i'm parameter.
 * Return: Always 0 (Success)
 */
void print_square(int size)
{
	int j;
	int x = 35;

	if (size > 0)
	{
		for (j = 0; j < size; j++)
		_putchar(x);
		_putchar('\n');
	}
	if (j == 0)
		_putchar('\n');
}
