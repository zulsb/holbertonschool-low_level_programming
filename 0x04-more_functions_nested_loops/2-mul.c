#include "holberton.h"

/**
 * mul - Multiplication two integers and returns the result.
 *@a: First variable
 *@b: Second variable
 * Return: Multiplication two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
