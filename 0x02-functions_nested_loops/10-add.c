#include "holberton.h"

/**
 * add - Adds two integers and returns the result..
 *@x: First variable
 *@y: Second variable
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
	return (x + b);

}
