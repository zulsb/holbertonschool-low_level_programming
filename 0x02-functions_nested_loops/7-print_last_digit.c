#include "holberton.h"

/**
 * print_last_digit - Last digit of a number.
 * @r: Parameter
 * Return: Always 0 (Success)
 */
int print_last_digit(int r)
{
	int r;

	r = abs(n % 10);
	_putchar('0' + r);

	return (r);
}
